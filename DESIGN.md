# DESIGN.md — デザインの意図

ココデザイン「印刷用QRコード変換」のデザイン方針と、その理由をまとめます。

---

## 1. 誰に向けたデザインか

見る人は、印刷会社・DTPオペレーター・デザイナーです。
この人たちは毎日Illustratorを開き、入稿データの不備を見つけるのが仕事です。
つまり **「よく作られたもの」と「それらしく見せているだけのもの」の区別がつく人たち** です。

そのため、次の4つを最初の数秒で伝えることを目標にしました。

1. 現場を理解している
2. 余計な機能がない
3. すぐ試せそう
4. 印刷データとして信頼できそう

派手さで注意を引く必要はありません。むしろ派手であること自体が信頼を損ないます。

---

## 2. 全体の方向性

| 参照 | 何を借りたか |
| --- | --- |
| 無印良品 | 整理された余白、色数の少なさ、説明の淡々とした調子 |
| NHK「デザインあ」 | 構造をそのまま見せる楽しさ。グリッド、通し番号、要素の反復 |

「デザインあ」的な要素は、装飾ではなく **情報の骨格を見せる方向** に使っています。
具体的には、セクションの通し番号（`04` `05` …）、うっすらとした方眼の地、
ロゴの2×2モジュール、変換ツール内の等幅フォントによる数値表示です。
アニメーションは変換中のバーとFAQの＋記号の回転だけに留めました。

---

## 3. 色

白を基調とし、黒・グレー・K100を中心に構成しています。

| トークン | 値 | 用途 |
| --- | --- | --- |
| `paper` | `#FFFFFF` | 背景 |
| `ink` | `#111111` | 本文・見出し |
| `k100` | `#000000` | QRの黒。印刷でのK100を意味する |
| `sumi-50 〜 700` | `#FAFAF9` 〜 `#3A3A35` | 罫線、補足、面の切り替え |
| `shu`（朱色） | `#B4482F` | 注意、フォーカスリング、Afterの印 |
| `aomidori`（青緑） | `#2C6B62` | 確認済み・チェック通過 |
| `kinari`（淡い黄） | `#F2E7BE` | 見出しの下線、補足のハイライト |

### 決めごと

- **補助色は面積を最小にする。** 朱色と青緑は点・線・小さな文字にだけ使い、面には使いません。
  「K100が正しく出ているか」を見る目に対して、余計な色は情報のノイズになるためです。
- **QRの黒だけは純黒（`#000000`）。** 本文の黒（`#111111`）とは意図的に分けています。
  QRは「印刷でK100になるもの」であり、UIのテキストとは意味が違うためです。
- **グラデーションは原則使いません。重要な場所を強調する場合に限って、使用を検討します。**
  現在使っているのはトップの置き場（生成りのトレイ、§10）だけです。
  ネオン、発光表現は使いません。AI的な演出は、出力データの正確さを売るサービスの主張と矛盾します。

---

## 4. 文字

日本語はシステムフォントのみで組んでいます（Webフォントは1つも読み込みません）。

```
-apple-system → Hiragino Kaku Gothic ProN → Hiragino Sans →
Noto Sans JP → BIZ UDPGothic → Yu Gothic → Meiryo
```

理由は3つあります。

1. 表示が速い。ヒーローの文字が遅れて出ない。
2. 対象ユーザーの環境（Mac中心、一部Windows）で確実にきれいに出る。
3. フォントで個性を出す必要がない。個性は情報の正確さで出す。

- `font-feature-settings: 'palt'` を有効にし、日本語の詰めを自然にしています。
- 数値・型番・ファイル名・技術的なラベルは **等幅フォント** に統一しました。
  「これは読むものではなく確認するもの」という区別を、書体で示しています。
- 本文の行間は `1.75〜2.0`。日本語は行間を詰めると急に読みにくくなります。
- 見出しは `tracking-japanese`（`0.02em`）でわずかに開けています。

---

## 5. 形

- **角丸の最大値は 2px。** Tailwind の `borderRadius` を上書きし、大きな角丸を使えないようにしています。
  角丸は「やわらかさ」を出す装飾ですが、このサイトで伝えたいのは正確さです。
  使い分けは次のとおりです。
  - **操作する要素（ボタン、入力欄、セレクト、タブ、選択カード）→ 2px。**
    触れる対象であることを、ごくわずかな丸みで示します。
  - **面・区画（パネル、カード、表組み、罫線で囲った領域）→ 0px。**
    紙面の版面に近い見え方にし、情報の境界を直角で示します。
- **影はほぼ使わない。** 使うのは面の境界を示す `shadow-panel`（1px相当）と、
  置き場のくぼみを示す内側の影（`shadow-tray`）だけです。
  奥行きの表現より、罫線による区画の明示を優先しました。
- **罫線で区切る。** カード群は `gap-px` + 背景色で1pxの罫線に見せています。
  印刷物の表組みに近い見え方で、DTPの人には馴染みのある構造です。

---

## 6. 情報設計（初版 `/v1/` の設計）

> この章と次章は、**初版（`/v1/`）** の設計を記録したものです。
> 現行のトップページは §10 の「置くだけ」の構成に置き換わっています。
> 初版の判断も、そこへ至る前提として残しておきます。

### 並び順

```
ヘッダー → ファーストビュー → 変換フォーム → 用語 → 特長 → 困りごと
→ 使い方 → 変換例 → 料金 → 対象ユーザー → FAQ → フッター
```

**変換フォームをファーストビューの直下に置いた** ことが最大の判断です。
料金や機能説明より先に、実際に触れる場所を出しています。
「説明を読ませて納得させる」のではなく「1枚入れてみれば分かる」構造にしました。

### 専門用語の扱い

K100、CMYK、RGB、ベクター、PDF、SVG、PNG、Illustrator、ドット配置、リンク画像、印刷用データ
—— これらは **言い換えず、そのまま使っています。**
対象ユーザーにとっては日常語であり、平易な言葉に置き換えるとかえって「分かっていない人が作った」と伝わります。

そのうえで、初見の人のために短い補足を添えています。

- 用語のすぐ横に括弧書きで一行（例：ベクター（拡大しても崩れない））
- 「このページで使う言葉」セクションに11語をまとめて一覧化
- FAQで、K100とRGBについては理由まで説明

### ファーストビューのBefore / After

**Beforeの画像は加工した見た目ではなく、実際に低解像度化したQR画像です。**
QRのマトリクスを面積平均で 1.7px/モジュール のラスターに落とし、
それをブラウザで拡大表示しています。
モジュール境界とピクセル境界がずれて中間調が生まれる、支給画像で実際に起きる現象がそのまま出ます。

AfterはSVGのベクター描画です。実際の出力（PDF・SVG）と同じ、行単位でまとめた矩形パスで描いています。
**両方とも実際に読み取れるQRです。** 装飾用のQR風の模様は1つも使っていません。

---

## 7. 変換フォームの設計（初版 `/v1/`）

初版におけるページの中心です。次を意識しました。

- **アップロードエリアを大きく取る。** 何をする場所かを形で示します。
- **タブは2つだけ。** 「画像から変換」「URL・テキストから生成」。機能を増やしません。
- **設定は4つだけ。** 出力形式 / 仕上がりサイズ / 誤り訂正レベル / 3つのチェックボックス。
- **状態が常に見える。** 解析中・変換中・完了を、文字とバーの両方で示します。
  ボタンは無効時に背景色ごと変わり、押せるかどうかが一目で分かります。
- **エラーはフォームの直下に出す。** 対応形式、ファイルサイズ、空入力、文字数超過を検証しています。

### 誠実さのための設計

- 当初は画像の読み取りがモックだったため、それを **画面上で明示** し、読み取り内容を手で
  修正できるようにしていました。「AIが自動で全部やります」と見せかけない、という判断です。
  現在は読み取りを実装したためモック表記は外していますが、**修正できる入力欄は残しています**
  （誤読時の救済として、いまも意味があるため）。
- 「SVGはRGBのみの規格のため、K100はPDF出力時に適用されます」という注記を入れています。
  正確に言えばSVGにCMYKは存在しません。ここを曖昧にすると、分かっている人にはすぐ伝わります。
- 変換完了後に「印刷前に実データでテストしてください」と必ず表示します。

---

## 8. レスポンシブ

モバイルファーストで組んでいます。

| 幅 | 主な変化 |
| --- | --- |
| 〜767px | 1カラム。ナビゲーションは折りたたみ。ボタンは全幅。困りごとは縦積み |
| 768px〜 | ヒーローが2カラム。カードが3列。困りごとが左右対比 |
| 1024px〜 | 変換例が3カラム。使い方が4列 |

- タップ対象はすべて 40px 以上の高さを確保しています。
- 変換フォームの設定欄は、狭い画面では縦一列に落ちます。
- 数値の並ぶ定義リストは、狭い画面でも折り返さないよう桁幅を固定しています。
- 日本語のテキストは `overflow-wrap: anywhere` と `line-break: strict` を併用し、
  禁則を守りつつ長いURLでもはみ出さないようにしています。

---

## 9. やらなかったこと

意図的に外した要素です。

- **「近日公開」カードを並べない。** 未完成の機能を数で見せるのは信頼を下げます。
- **QR以外のDTPツールをナビゲーションに出さない。** 初版はQRだけに絞ります。
- **お客様の声・導入企業ロゴを置かない。** 実績がない段階での掲載は不誠実です。
- **数値の誇張をしない。** サイトに出ている「セル数」「矩形パス数」は、
  実際にサンプルQRをエンコードして算出した値です。固定文字列ではありません。
- **チャットボット、ポップアップ、スクロール追従バナーを置かない。** 作業の邪魔になります。

---

## 10. 「置くだけ」のファーストビュー（現行・`/`）

初版（現在は `/v1/` に保存）を見たうえでの壁打ちで、方針が一段進みました。
2026-09-06 にこの構成をトップページへ昇格させ、初版は比較用に `/v1/` へ移しました。
**デザイナーが右脳で作業しているときに、左脳（言語・判断）へ切り替えさせない。**
時短にならなくても、手が止まらないから使い続けてしまう——そういう道具にする、という方針です。

これを操作のルールに翻訳すると三つになります。

| ルール | 意味 | 実装 |
| --- | --- | --- |
| **打たない** | キーボードに手を伸ばさせない | `⌘V` でクリップボードの画像も文字列も受け付ける。ドロップ、クリック（タップ）も可 |
| **読まない** | 文章で状況を伝えない | 状態は「粗い画像が左から右へ鮮明になる」動きで見せる。説明文・注記は静止状態に置かない |
| **選ばない** | 判断を求めない | 形式・サイズ・K100・配置維持・内容照合は全部固定。ボタンは「受け取る」ひとつ |

### 画面にあるもの（静止状態）

見出し1行、置く場所（生成りのトレイと `⌘V または ドロップ`）、「試してみる →」、`URL または テキスト` のプレースホルダーを持つ1行、ヘッダーの「無料」。それだけです。
ナビゲーション、サブコピー、用語の補足、バッジ、注意書きはすべて外しました。

見出しは「QRコードを、印刷用のK100ベクターに。」。行動の案内（置くだけで）は置き場に任せ、
見出しは成果物の名前だけを言います。「K100ベクター」の一語が、印刷を知っている人への合図です。

### 置き場は「トレイ」

点線の四角は UI の約束事で、読んで理解するものです。くぼみは物なので、見れば分かります。
「読まない」の方針に、いちばん効くのがここです。

- **色は生成り（`kinari`）。** 灰色は「画面の部品」に見え、生成りは「紙」に見えます。
  QR が現れたとき「白い紙をトレイに置いた」と読めるのは生成りのほうです。
- **縁が沈む浅いトレイ。** 中心が暗い穴にすると、そこに現れる黒い QR と喧嘩します。
  上辺の内側の影（`shadow-tray`）と、上から下へ紙の白に近づくグラデーション（`bg-tray`）で作っています。
- **点線は外しました。** くぼみが「ここに置く」を言っているのに、点線でもう一度言うと二重になります。
- **ホバーとドラッグ中は、色を変えずにトレイが少し沈みます**（`shadow-tray-deep`）。
  ドラッグ中だけ枠線が朱になり、受け付けていることを示します。
- **置いた瞬間に白い紙へ溶けます。** トレイは待機中の姿であって、結果の背景ではありません。

### 1行目のプレースホルダー

`https://` は開発者の言葉で、テキストも入れられることが伝わりませんでした。
「URL または テキスト」は、置き場の「⌘V または ドロップ」と同じ語法です。
上は画像の入れ方、下は文字の入れ方。動詞も文もなく、ふたつが対で読めます。

### 「QRの中身」の1行

読み取り結果の**表示欄**であり、URL・テキストの**入力欄**でもあります。

- 画像を置く → 読み取った内容が入る（印刷現場で一番怖い「飛び先違い」の確認になる）
- 行に打つ／貼る → 上にQRが現れる

タブで「画像から」「テキストから」を選ばせない、というのがこの1行の役割です。
画像を置いたあとに書き換えたら、それはもう再生成です。そのとき「ドット配置」のラベルが灰色に変わり、
「（再生成）」と添える。良いことも悪いことも、言葉で説明せず状態で示します。

### 二つの層

「専門用語を隠さない」「印刷データとして信頼できそう」という当初の要件は、左脳の話です。
現行版では層を分けて両立させています。

- **感覚の層（表面）**：置く → 鮮明になる → 受け取る。
- **確認の層（一段奥）**：「詳細」を開くと、セル数・型番・誤り訂正・CMYK 0/0/0/100・内容一致・元画像の画素数が等幅で並ぶ。

入稿前の確認は、デザイナー自身が左脳に切り替える瞬間です。その瞬間のために数値は残し、こちらから押しつけません。

### 言葉に関する決めごと

- 会社に向けて言うのは「時短」。タグライン「毎日の5分を30秒に。」は残す（稟議が通る言葉だから）
- デザイナーに感じさせるのは「左脳を使わない」。**これは書かない。** サイトのどこにも「感覚的」「考えなくていい」「左脳」とは書かない。心地よさを説明した瞬間に宣伝になるため
- 「置くだけ」は操作の案内としてだけ使い、売り文句にしない
- 「QR画像」とは書かない。誰も使わない言葉なので「QRコード」に統一する（ファイルを指すときの「画像を選ぶ」は可）

### 変換前の見た目は加工ではない

「Before」に出るのは、置かれた画像そのものからシンボル部分を射影補正で切り出したものです。
粗さ・にじみ・JPEG のノイズはそのまま残ります。鮮明になる側は、元画像から取り出した
モジュール配置（誤差ゼロで確定できた場合）をベクターで描いたものです。演出のために画像を作っていません。

### この版で捨てたもの

初版の変換フォームにあった、出力形式の3択、仕上がりサイズ、誤り訂正レベル、3つのチェックボックス、
「変換する」ボタン、「変換完了」の文字。すべて現行版にはありません（`/v1/` で見比べられます）。
SVG・PNG は「登録後に受け取れます」の一文だけにし、無料の体験は削らずに登録の理由だけを作っています。

---

## 11. 今後変える可能性がある点

- 料金は仮です。3プランの内容と価格が決まった時点で、`src/lib/content/sections.ts` を更新します。
- 画像からのQR読み取りが本実装になったら、変換フォームの「モック版」バッジと
  読み取り内容の編集欄の扱いを見直します（編集欄自体は、誤読時の救済として残す想定です）。
- 一括変換が実装されても、トップに選択肢を足さず別ページに分けることを検討します。
  1画面あたりの判断を増やさない、というのが現行版の骨子だからです。
- 初版（`/v1/`）は比較のために残していますが、役目を終えたと判断した時点で削除します。
